!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'Friends Score'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Friends Score'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'

    .container{:role => "main"}
      = render 'layouts/messages'
      = yield

    .footer
      .container
        %p.text-muted.small
          ©#{Date.today.year}
          = link_to "Good Bit Labs", "http://goodbitlabs.com/"
